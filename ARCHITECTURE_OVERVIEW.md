# FeelUp Architecture Overview

## 🏗️ Current Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                         CLIENT LAYER                             │
├─────────────────────────────────────────────────────────────────┤
│                                                                   │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐          │
│  │  Mood Feed   │  │   Journal    │  │  Analytics   │          │
│  │    Page      │  │    Page      │  │    Page      │          │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘          │
│         │                  │                  │                   │
│         └──────────────────┴──────────────────┘                  │
│                            │                                      │
│                            ▼                                      │
│  ┌─────────────────────────────────────────────────────┐        │
│  │              COMPONENT LAYER                         │        │
│  ├─────────────────────────────────────────────────────┤        │
│  │  • MoodInput      • PostCard    • RightSidebar      │        │
│  │  • AuthCard       • Navbar      • Footer            │        │
│  │  • ChatBox        • Sidebar     • StreakDisplay     │        │
│  └─────────────────────┬───────────────────────────────┘        │
│                        │                                          │
└────────────────────────┼──────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│                      MIDDLEWARE LAYER                            │
├─────────────────────────────────────────────────────────────────┤
│                                                                   │
│  ┌────────────────────────────────────────────────────────┐    │
│  │  middleware.ts                                          │    │
│  │  • Authentication Check                                 │    │
│  │  • Role-based Routing (admin/psychologist/user)        │    │
│  │  • Session Management                                   │    │
│  └────────────────────────────────────────────────────────┘    │
│                                                                   │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│                       API LAYER                                  │
├─────────────────────────────────────────────────────────────────┤
│                                                                   │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐          │
│  │ detect-mood  │  │ mood-posts   │  │   journal    │          │
│  │   /route.ts  │  │  /route.ts   │  │  /route.ts   │          │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘          │
│         │                  │                  │                   │
│         │                  │                  │                   │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐          │
│  │  comments    │  │  reactions   │  │   follow     │          │
│  │  /route.ts   │  │  /route.ts   │  │  /route.ts   │          │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘          │
│         │                  │                  │                   │
│         └──────────────────┴──────────────────┘                  │
│                            │                                      │
└────────────────────────────┼──────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                      SERVICE LAYER                               │
├─────────────────────────────────────────────────────────────────┤
│                                                                   │
│  ┌────────────────────┐  ┌────────────────────┐                │
│  │  supabaseClient.ts │  │   cloudinary.ts    │                │
│  │  • Browser Client  │  │   • Image Upload   │                │
│  │  • Server Client   │  │   • Image Storage  │                │
│  └─────────┬──────────┘  └──────────┬─────────┘                │
│            │                         │                            │
│  ┌─────────┴──────────┐  ┌──────────┴─────────┐                │
│  │     cache.ts       │  │   (OpenAI API)     │                │
│  │  • In-memory Cache │  │  • Mood Detection  │                │
│  └────────────────────┘  └────────────────────┘                │
│                                                                   │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│                      DATA LAYER                                  │
├─────────────────────────────────────────────────────────────────┤
│                                                                   │
│  ┌──────────────────────────────────────────────────────┐      │
│  │              Supabase (PostgreSQL)                    │      │
│  │  ┌────────────┐  ┌────────────┐  ┌────────────┐     │      │
│  │  │ mood_posts │  │  profiles  │  │  comments  │     │      │
│  │  └────────────┘  └────────────┘  └────────────┘     │      │
│  │  ┌────────────┐  ┌────────────┐  ┌────────────┐     │      │
│  │  │ reactions  │  │   goals    │  │  journal   │     │      │
│  │  └────────────┘  └────────────┘  └────────────┘     │      │
│  └──────────────────────────────────────────────────────┘      │
│                                                                   │
│  ⚠️  ISSUE: Also using file-based storage (data/mood_posts.json)│
│                                                                   │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│                   EXTERNAL SERVICES                              │
├─────────────────────────────────────────────────────────────────┤
│                                                                   │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐          │
│  │   OpenAI     │  │  Cloudinary  │  │   Supabase   │          │
│  │  GPT-4o-mini │  │Image Storage │  │   Auth/DB    │          │
│  └──────────────┘  └──────────────┘  └──────────────┘          │
│                                                                   │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🔄 Data Flow Examples

### 1. Creating a Mood Post

```
User types mood → MoodInput component
                     │
                     ▼
        Debounced AI detection (700ms)
                     │
                     ▼
        POST /api/detect-mood
                     │
                     ▼
        ❌ WRONG: /v1/responses endpoint
        ✅ SHOULD: /v1/chat/completions
                     │
                     ▼
        OpenAI returns mood + confidence
                     │
                     ▼
        Display detected mood to user
                     │
                     ▼
        User clicks "Share ✨"
                     │
                     ▼
        POST /api/mood-posts
                     │
                     ├──▶ ⚠️ ISSUE: Writes to JSON file
                     │
                     └──▶ ✅ SHOULD: Write to Supabase
                           │
                           ▼
                  Supabase mood_posts table
                           │
                           ▼
                  Real-time update to feed
```

### 2. Authentication Flow

```
User visits protected route
         │
         ▼
middleware.ts intercepts
         │
         ▼
Check Supabase session
         │
         ├──▶ No session → Redirect to /login
         │
         └──▶ Has session
              │
              ▼
         Check user role (email-based)
              │
              ├──▶ @admin.feelup → /admin
              ├──▶ @psychologist.feelup → /psychologist
              └──▶ other → user routes
```

### 3. Image Upload Flow

```
User selects image
         │
         ▼
Convert to base64
         │
         ▼
POST /api/mood-posts
         │
         ▼
⚠️ ISSUE: Cloudinary code commented out
         │
         ├──▶ ❌ Currently: Image not uploaded
         │
         └──▶ ✅ Should: Upload to Cloudinary
                    │
                    ▼
              Get secure_url
                    │
                    ▼
              Store URL in database
```

---

## 🚨 Architecture Issues

### 1. Dual Database Problem

```
Current (WRONG):
┌─────────────┐     ┌─────────────┐
│  Supabase   │     │  JSON File  │
│  (Partial)  │     │  (Partial)  │
└─────────────┘     └─────────────┘
      ↑                    ↑
      └────────┬───────────┘
               │
         Inconsistent!
```

```
Should Be:
┌─────────────┐
│  Supabase   │
│   (All)     │
└─────────────┘
      ↑
      │
   Single source of truth
```

### 2. Caching Problem

```
Current (WRONG):
┌──────────────────┐
│  In-memory Map   │  ← Resets on serverless cold start
│  No TTL          │  ← Cache grows forever
│  FIFO eviction   │  ← Not optimal
└──────────────────┘
```

```
Should Be:
┌──────────────────┐
│  Redis / LRU     │  ← Persistent across instances
│  With TTL        │  ← Auto-expiration
│  LRU eviction    │  ← Optimal performance
└──────────────────┘
```

### 3. Error Handling Gap

```
Current (WRONG):
Component Error → App Crashes 💥
```

```
Should Be:
Component Error → Error Boundary → Fallback UI ✅
```

---

## 🎯 Recommended Architecture Improvements

### Phase 1: Fix Critical Issues

```
1. Security
   ├─ Rotate API keys
   ├─ Remove from git history
   └─ Add .env.example

2. Core Functionality
   ├─ Fix OpenAI endpoint
   ├─ Run database migration
   └─ Remove file-based storage

3. Error Handling
   ├─ Add error boundaries
   ├─ Add input validation
   └─ Improve error messages
```

### Phase 2: Quality Improvements

```
1. Type Safety
   ├─ Create type definitions
   ├─ Remove 'any' types
   └─ Enable strict mode

2. Testing
   ├─ Add unit tests
   ├─ Add integration tests
   └─ Set up CI/CD

3. Performance
   ├─ Add React Query
   ├─ Optimize images
   └─ Add proper caching
```

### Phase 3: Advanced Features

```
1. Monitoring
   ├─ Add Sentry
   ├─ Add analytics
   └─ Add logging

2. Optimization
   ├─ Add PWA support
   ├─ Add compression
   └─ Add CDN

3. Scalability
   ├─ Add rate limiting
   ├─ Add load balancing
   └─ Add database indexes
```

---

## 📊 Component Dependency Graph

```
App Layout
├── Navbar
│   ├── Logo
│   └── NotificationsBell
│
├── Main Content Area
│   ├── LeftSidebar
│   │   └── Navigation Links
│   │
│   ├── Page Content
│   │   ├── MoodInput (mood-feed)
│   │   ├── PostCard (mood-feed)
│   │   ├── ChatBox (messages)
│   │   └── StreakDisplay (analytics)
│   │
│   └── RightSidebar
│       ├── Suggested Profiles
│       ├── Communities
│       └── Goals
│
└── Footer
    └── Links & Info
```

---

## 🔐 Security Architecture

### Current State (VULNERABLE)

```
┌─────────────────────────────────────┐
│  .env.local (IN GIT REPO!) ❌      │
│  ├─ OPENAI_API_KEY                  │
│  ├─ SUPABASE_SERVICE_ROLE_KEY       │
│  ├─ CLOUDINARY_API_SECRET           │
│  └─ OAuth Secrets                   │
└─────────────────────────────────────┘
         │
         ▼
    Anyone with repo access
    can steal credentials! 🚨
```

### Recommended State (SECURE)

```
┌─────────────────────────────────────┐
│  .env.local (GITIGNORED) ✅        │
│  (Local development only)           │
└─────────────────────────────────────┘

┌─────────────────────────────────────┐
│  .env.example (IN GIT) ✅          │
│  (Template with placeholders)       │
└─────────────────────────────────────┘

┌─────────────────────────────────────┐
│  Vercel Environment Variables ✅   │
│  (Production secrets)               │
└─────────────────────────────────────┘
```

---

## 🎨 File Structure

```
FeelUp-main/
├── app/
│   ├── (app)/              # User-facing pages
│   │   ├── mood-feed/
│   │   ├── journal/
│   │   ├── analytics/
│   │   └── ...
│   │
│   ├── (staff)/            # Admin/Psychologist pages
│   │   ├── admin/
│   │   └── psychologist/
│   │
│   ├── api/                # API routes
│   │   ├── detect-mood/    ❌ BROKEN
│   │   ├── mood-posts/     ⚠️  DUAL STORAGE
│   │   ├── journal/
│   │   └── ...
│   │
│   ├── globals.css
│   ├── layout.tsx
│   └── page.tsx
│
├── components/             # Reusable components
│   ├── MoodInput.tsx       🐛 Debug logs
│   ├── PostCard.tsx
│   ├── RightSidebar.tsx
│   └── ...
│
├── lib/                    # Utilities
│   ├── supabaseClient.ts   ✅ Good
│   ├── cache.ts            ⚠️  Inefficient
│   └── cloudinary.ts
│
├── types/                  # Type definitions
│   └── next-auth.d.ts      ⚠️  Incomplete
│
├── .env.local              🚨 EXPOSED!
├── .env.example            ❌ MISSING
├── middleware.ts           ✅ Good
├── next.config.ts          ⚠️  Incomplete
├── tsconfig.json           ✅ Good
└── package.json            ✅ Good
```

---

## 📈 Performance Bottlenecks

### 1. No Data Caching
```
Every request → Database query
No caching layer
No stale-while-revalidate
```

### 2. Unoptimized Images
```
Images loaded at full size
No lazy loading
No blur placeholders
No responsive images
```

### 3. No Code Splitting
```
All components loaded upfront
No dynamic imports
Large bundle size
```

### 4. Inefficient Queries
```
No database indexes on common queries
N+1 query problems
No pagination
```

---

## 🎯 Success Metrics

Track these after implementing fixes:

```
┌─────────────────────────────────────┐
│  Performance Metrics                │
├─────────────────────────────────────┤
│  • First Contentful Paint: < 1.5s   │
│  • Time to Interactive: < 3.5s      │
│  • Largest Contentful Paint: < 2.5s │
│  • Cumulative Layout Shift: < 0.1   │
└─────────────────────────────────────┘

┌─────────────────────────────────────┐
│  Quality Metrics                    │
├─────────────────────────────────────┤
│  • Test Coverage: > 80%             │
│  • TypeScript Errors: 0             │
│  • ESLint Errors: 0                 │
│  • Security Score: A+               │
└─────────────────────────────────────┘

┌─────────────────────────────────────┐
│  User Experience Metrics            │
├─────────────────────────────────────┤
│  • Accessibility Score: > 95        │
│  • Mobile Performance: > 90         │
│  • SEO Score: > 90                  │
│  • PWA Score: > 80                  │
└─────────────────────────────────────┘
```

---

This architecture overview should help you understand the current structure and where improvements are needed!
